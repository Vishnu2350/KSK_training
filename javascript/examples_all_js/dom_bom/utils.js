export function greetingMessage(name) {
  return `Hi, ${name}!`;
}
export function displayMailCounts(counts) {
  return `You have ${counts.unread} unread messages and ${counts.total} total messages.`;
}

/* Operation 1: Working with Objects */

const car = {
    brand: "Toyota",
    model: "Corolla",
    year: 2021,
  };
  
  console.log(car.brand);  // Accessing object property

  /* Operation 2: Working with Arrays*/

  const fruits = ["Apple", "Banana", "Cherry"];
console.log(fruits[1]);  // Accessing array element

/* Operation 3: Array Methods */

const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(num => num * 2);
console.log(doubled);  // Using map method

  